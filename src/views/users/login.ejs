<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head', {title: 'Login'}) %>
    <body>
        
        <%- include('../partials/header') %>
        
        <main class="loginmain">
           
            <img src="/img/img_model_2.jpg" alt="imagen de una modelo" class="loginImage">

            <section class= "formulariologin">
                <h1 class="h1-size">Ingresar con mail y contraseña</h1>

                <form action="/usuario/login" method="POST">
                    <p><label for="email">Email : </label></p>
                    <p><input type="email" name="email" id="email" placeholder="Ej: maria@babieka.com" value="<%= typeof errors == 'undefined' ? '' : email%>"></p>
                    <p class="validationErrors"><%= typeof errors == 'undefined' ? '' : typeof errors.email == 'undefined' ? '' : errors.email.msg %></p>

                    <p><label for="password">Contraseña : </label></p>
                    <p><input type="password" name="password" id="password"></p>
                    <p class="validationErrors"><%= typeof errors == 'undefined' ? '' : typeof errors.password == 'undefined' ? '' : errors.password.msg %></p>

                    <div class="forgotPass">
                        <a href="">¿Olvidaste tu contraseña?</a>
                    </div>

                    <div class="rememberme">
                        <label for="remember">Recuérdame</label>
                        <input type="checkbox" id="remember" name="remember" style="width: 15%;">
                    </div>

                    <button type="submit">Ingresar</button>
                </form>

            </section>
            
        </main>
        
        <%- include('../partials/footer') %>
        
    </body>
</html>